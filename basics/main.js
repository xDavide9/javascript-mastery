// the file syntax is managed by eslint

console.log("Hello World!");

// javascript is a dynamically and weakly typed language
// the language is really similar to java in its features so only different things will be reported