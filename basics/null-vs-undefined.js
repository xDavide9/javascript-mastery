let something;

// undefined
console.log(something);

something = null;

// null
console.log(something);

// the difference is that null is assigned by the programmer as a value
// while undefined takes place when something just wasn't assigned any value
